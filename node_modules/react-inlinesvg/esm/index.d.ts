import * as React from 'react';
import { Props, State, StorageItem } from 'react-inlinesvg/esm/types';
export declare const cacheStore: {
    [key: string]: StorageItem;
};
export default class InlineSVG extends React.PureComponent<Props, State> {
    private isActive;
    private readonly hash;
    static defaultProps: {
        cacheRequests: boolean;
        uniquifyIDs: boolean;
    };
    constructor(props: Props);
    componentDidMount(): void;
    componentDidUpdate(previousProps: Props, previousState: State): void;
    componentWillUnmount(): void;
    private getNode;
    private getElement;
    private handleLoad;
    private handleError;
    private request;
    private load;
    private updateSVGAttributes;
    private processSVG;
    render(): React.ReactNode;
}
export * from 'react-inlinesvg/esm/types';
